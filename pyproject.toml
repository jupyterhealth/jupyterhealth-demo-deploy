[tool.black]
skip-string-normalization = true

[tool.isort]
profile = "black"

# ruff no longer used
[tool.ruff.format]
quote-style = "preserve"

[tool.ruff.lint]
ignore = [
    "F841", # unused variable
]
select = [
    "E9", # syntax
    "I", # isort
    "UP", # pyupgrade
    "F", # flake8
]
